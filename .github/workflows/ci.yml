name: CI

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Verify docs folder exists
        run: test -d docs

      - name: Ensure hummusapiens is not tracked
        run: |
          if git ls-files | grep -q '^hummusapiens/'; then
            echo "hummusapiens/ must not be tracked"
            exit 1
          fi

      - name: Validate Docker Compose config
        run: docker compose -f compose/docker-compose.yml config
